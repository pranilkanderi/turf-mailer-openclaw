import { Client, ClientOptions } from '@microsoft/teams.common/http';
import { MessageReactionType } from '../../models/message/message-reaction.js';
import { ApiClientSettings } from '../api-client-settings.js';
import '../../models/message/message-user.js';

/**
 * Client for adding and removing emoji reactions on messages in a conversation.
 *
 * @experimental This API is in preview and may change in the future.
 * Diagnostic: ExperimentalTeamsReactions
 */
declare class ReactionClient {
    readonly serviceUrl: string;
    get http(): Client;
    set http(v: Client);
    protected _http: Client;
    protected _apiClientSettings: Partial<ApiClientSettings>;
    constructor(serviceUrl: string, options?: Client | ClientOptions, apiClientSettings?: Partial<ApiClientSettings>);
    /**
     * Add a reaction to a message.
     *
     * @experimental This API is in preview and may change in the future.
     * Diagnostic: ExperimentalTeamsReactions
     */
    add(conversationId: string, activityId: string, reactionType: MessageReactionType): Promise<void>;
    /**
     * Remove a reaction from a message.
     *
     * @experimental This API is in preview and may change in the future.
     * Diagnostic: ExperimentalTeamsReactions
     */
    remove(conversationId: string, activityId: string, reactionType: MessageReactionType): Promise<void>;
}

export { ReactionClient };
