import { IToken } from '@microsoft/teams.api';
import { ILogger } from '@microsoft/teams.common';
/**
 * Service token validator for Bot Framework /api/messages requests
 * Validates Bot Framework service tokens
 */
export declare class ServiceTokenValidator {
    private jwtValidator;
    private credentials?;
    constructor(appId: string, tenantId?: string, serviceUrl?: string, logger?: ILogger);
    check(authHeader: string, body: any): Promise<IToken>;
}
