import { ActivityParams, ConversationReference, SentActivity } from '@microsoft/teams.api';
import * as $http from '@microsoft/teams.common/http';
import { ILogger } from '@microsoft/teams.common/logging';
import { IStreamer, IActivitySender } from './types';
/**
 * Handles sending activities to the Bot Framework
 * Separate from transport concerns (HTTP, WebSocket, etc.)
 */
export declare class ActivitySender implements IActivitySender {
    private client;
    private logger;
    constructor(client: $http.Client, logger: ILogger);
    send(activity: ActivityParams, ref: ConversationReference): Promise<SentActivity>;
    createStream(ref: ConversationReference): IStreamer;
}
